1) multi agent communication (explain)

2) demonistrate application (frontend to backend)
        start backend
            cd backend
            python -m venv venv
            source venv/bin/activate
            pip install -r requirements.txt
            uvicorn app.main:app --reload
        start frontend
            cd frontend
            npm install
            npm run dev
        http://localhost:5173

3) architecture with langgraph

4) coding snippets

-----------------------
1) build backend docker image
        backend
            Dockerfile

2) build frontend docker image
        frontend
            Dockerfile

3) create docker-compose.yml file
        provide communication between two docker containers
        (backend) (frontend)

4) push code to GitHub